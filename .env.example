# Optional runner settings
PORT=8787

# Optional custom location for the local credential vault.
# Relative paths are resolved from the project root; absolute paths are also accepted.
# GENESYS_ENVIRONMENTS_FILE=./config/environments.local.json
